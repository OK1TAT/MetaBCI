# -*- coding: utf-8 -*-
"""
全局配置文件

所有可调参数集中管理，避免硬编码。
"""

import os

# ============================================================
# 数据路径配置（根据实际路径修改）
# ============================================================
HC_DIR = r"D:\metabci_3class\用户上传\ALL_Noraml_Data"          # HC组EDF目录
DEP_DIR = r"D:\metabci_3class\用户上传"                         # 抑郁症组EDF目录
LABEL_EXCEL = r"D:\metabci_3class\用户上传\名单.xlsx"           # 抑郁症名单

# ============================================================
# 通道配置
# ============================================================
# 前额六导联（大写，upper_ch_names 处理后）
FRONTAL_CHANNELS = ['FP1', 'FP2', 'F3', 'F4', 'F7', 'F8']

# 采样率
RESAMPLE_FREQ = 250        # 重采样目标频率 (Hz)

# ============================================================
# MoCA 评分配置
# ============================================================
MOCA_THRESHOLD = 26        # MoCA 阈值：<26 为 D-CI，>=26 为 D-NCI

# ============================================================
# 特征提取配置
# ============================================================
# 频段设置（用于 PLI/RP/FE）
# theta, alpha_low, alpha_high, beta_low, beta_high
PASSBANDS = [[4, 8], [8, 10], [10, 13], [13, 20], [20, 30]]
STOPBANDS = [[2, 10], [6, 12], [8, 15], [11, 22], [18, 32]]

# 滤波器参数（generate_filterbank）
FILTERBANK_ORDER = 4       # Chebyshev Type I 阶数
FILTERBANK_RP = 0.5        # 通带波纹(dB)

# 模糊熵参数
FUZZY_M = 2                # 嵌入维度
FUZZY_R = 0.2              # 相似度容限（比例系数）
FUZZY_N = 2                # 模糊隶属函数指数

# ============================================================
# 分类配置
# ============================================================
RANDOM_SEED = 42           # 随机种子
N_FOLDS = 5                # 交叉验证折数

# 类别标签映射
# 0: D-CI (抑郁认知障碍)
# 1: D-NCI (抑郁认知正常)
# 2: HC (健康对照)
CLASS_NAMES = {
    0: "D-CI",
    1: "D-NCI",
    2: "HC"
}

# ============================================================
# 输出配置
# ============================================================
_PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
OUTPUT_DIR = os.path.join(_PROJECT_ROOT, "results_3class")

# ============================================================
# 采集配置（静息态范式）
# ============================================================
REST_DURATION = 60         # 静息采集时长（秒）
PORT_ADDR = None           # 并口地址，如 0xdefc 或 "COM8"
SCREEN_SIZE = [1920, 1080] # 屏幕分辨率
